.center
  %h1 Categories

%table.table
  %tr
    %th Name
    %th
    %th
    %th

  - categories.each do |category|
    %tr
      %td= category.name
      %td= link_to 'Show', category, class: "btn btn-primary"
      - if user_signed_in? && current_user.admin?
        %td= link_to 'Edit', edit_category_path(category), class: "btn btn-warning"
        %td= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger"

%br
- if user_signed_in? && current_user.admin?
  = link_to 'New Category', new_category_path, class: 'btn btn-success'
- else
  %p
    %em You are not an admin, you cannot create, edit and destroy categories.
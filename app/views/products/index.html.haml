%h1 Listing products

%table.table.table-striped.table-hover
  %tr
    %th Name
    %th Description
    %th Price
    %th Average Rating
  - category.products.each do |product|
    %tr{:data => { :'link' => category_product_path(product.category_id, product) }}
      %td
        = product.title 
      %td
        = product.description
      %td
        = product.price
      %td
        = product.average_rating.round(2) if product.average_rating != nil

.col-md-6
  %h3 New product
  = render 'products/form'
  = link_to 'Back', categories_path, class: 'btn btn-primary'